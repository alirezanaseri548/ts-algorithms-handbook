# Step 31: Singly Linked List Read

- Added `read(index)` to access a node value by position.
- Reading from a singly linked list takes O(n) time in the general case.
- Negative or out-of-range indexes return `null`.