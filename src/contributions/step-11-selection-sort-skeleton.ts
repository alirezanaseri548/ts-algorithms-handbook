/**
 * Step 11: Selection Sort Skeleton
 *
 * Goal:
 * Create the basic function structure for Selection Sort.
 *
 * Selection Sort idea:
 * - Find the smallest item in the unsorted part.
 * - Move it to the beginning of that part.
 * - Repeat until the array is sorted.
 */

export function selectionSortSkeleton(items: number[]): number[] {
  const result = [...items];

  return result;
}

console.log(selectionSortSkeleton([64, 25, 12, 22, 11]));