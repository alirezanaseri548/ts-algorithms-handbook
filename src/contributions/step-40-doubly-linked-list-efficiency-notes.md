# Step 40: Doubly Linked List Efficiency

A doubly linked list keeps both previous and next links.

- Removing from the front is O(1).
- Removing from the end is O(1).
- A queue can use append + removeFromFront for efficient FIFO behavior.
- Arrays may need shifting when deleting from the front.