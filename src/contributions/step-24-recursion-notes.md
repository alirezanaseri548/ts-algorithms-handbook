# Step 24: Recursion

Recursion solves a problem by letting a function call itself with a smaller input.

## Factorial

The factorial function has two important parts:

- Base case: `0!` and `1!` return `1`
- Recursive case: `n! = n * factorial(n - 1)`
