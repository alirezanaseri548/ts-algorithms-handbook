/**
 * Step 15: Insertion Sort Skeleton
 *
 * Goal:
 * Create the basic function structure for Insertion Sort.
 *
 * Insertion Sort idea:
 * - Start from the second item.
 * - Take the current item as a key.
 * - Compare it with previous items.
 * - Shift larger items to the right.
 * - Insert the key into its correct position.
 */

export function insertionSortSkeleton(items: number[]): number[] {
  const result = [...items];

  return result;
}

console.log(insertionSortSkeleton([5, 2, 4, 6, 1, 3]));
