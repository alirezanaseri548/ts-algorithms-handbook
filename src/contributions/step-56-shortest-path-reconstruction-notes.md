# Step 56 — Shortest-Path Reconstruction

A predecessor array produced by a shortest-path algorithm can be converted into an explicit path.

- Returns `null` when the target is unreachable.
- Detects cyclic predecessor chains.
- Validates predecessor indexes.
